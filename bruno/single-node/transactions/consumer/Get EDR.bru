meta {
  name: Get EDR
  type: http
  seq: 5
}

get {
  url: {{MANAGEMENT}}/v3/edrs/{{transferId}}/dataaddress
  body: none
  auth: apikey
}

headers {
  Accept: application/json
}

auth:apikey {
  key: X-Api-key
  value: {{MANAGEMENT_API_KEY}}
  placement: header
}

script:post-response {
  const authToken = res.getBody().authorization; 
  bru.setEnvVar("pullSecret", authToken);
}

settings {
  encodeUrl: true
  timeout: 0
}
