# Local Testbed (Docker)

End-to-end local testbed running two Connectors (consumer and provider) plus wallets and shared
infrastructure, for testing full data space interactions on a single machine.
